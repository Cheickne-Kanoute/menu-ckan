{
  "scripts": {
    "build": "ncp src/css public/assets/css && ncp src/js public/assets/js && ncp src/data public/data",
    "dev": "echo \"Use a local server like live-server or http-server\""
  },
  "devDependencies": {
    "ncp": "^2.0.0"
  }
}